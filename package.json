{
  "name": "tadp-tpi",
  "private": true,
  "scripts": {
    "dev": "npm-run-all --parallel dev:turnos dev:chequeos",
    "dev:turnos": "cd backend/turnos && npm run dev",
    "dev:chequeos": "cd backend/chequeos && npm run dev"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
